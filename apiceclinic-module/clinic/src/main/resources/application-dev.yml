spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/apiceclinic}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:password} # TODO: move to environment variable
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
